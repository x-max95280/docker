version: '3'
services:
  nginx:
    build: .
    ports:
    - 8080:80
    volumes:
        - ./code:/code    
    links:
        - php
  php:
    image: php:7.3-fpm
    volumes:
        - ./code:/code

# version: '3'
# services:
  # web:
    # image: nginx
    # ports:
        # - "8180:80"
    # volumes:
        # - ./code:/code
        # - ./site.conf:/etc/nginx/conf.d/default.conf
    # links:
        # - php
  # php:
    # image: php:7.3-fpm
    # volumes:
        # - ./code:/code